# ملخص الإصلاحات المطبقة

تاريخ الإصلاح: 2026-07-07

## إصلاحات قاعدة البيانات

- إصلاح ترتيب إنشاء جدول `user_features_settings` بحيث يتم إنشاؤه قبل أي `ALTER TABLE` عليه.
- إضافة عمود `game_bonus_amount` إلى جدول `users` لحفظ البونص المحوّل إلى اللعبة والذي يحتاج تدوير.
- إضافة أعمدة إعدادات ناقصة في `user_features_settings`:
  - `checkin_min_deposit`
  - `wheel_min_deposit`
  - `wheel_max_reward`
- ضمان قراءة إعدادات العجلة/VIP/cashback المحفوظة وعدم تجاهلها.

## إصلاحات نظام المكافآت

- إصلاح SQL في `reduce_user_game_bonus`.
- توحيد إعدادات عجلة الحظ على 8 قطاعات كما تتوقع لوحة Dashboard.
- ضمان استمرار القيم المحفوظة للعجلة وVIP والكاش باك بعد الحفظ من لوحة المشرف.

## إصلاحات تجربة المستخدم

- تعديل `/start` بحيث لا تظهر القائمة الرئيسية للمستخدم العادي قبل قبول الشروط.
- توحيد نص الشروط بصيغة HTML صحيحة بدلاً من Markdown داخل رسائل HTML.

## تنظيف الكود والملفات

- إزالة ملفات `__pycache__` وملفات `.pyc` وملفات السجلات runtime.
- إضافة `.gitignore` مناسب للمشروع.
- تنظيف تحذيرات `pyflakes` غير الحرجة.

## الفحوصات التي تم تنفيذها

```bash
python -m compileall -q .
python -m pyflakes .
```

النتيجة: لا توجد أخطاء Syntax ولا تحذيرات pyflakes بعد التنظيف.

---

# تحديث نظام بونص اللعب المبسط

## الفكرة المطبقة

- البونصات من أي مصدر تبقى حقاً للمستخدم داخل `bonus_balance`.
- لا يمكن تحويل البونص وحده إلى اللعبة.
- عند شحن حساب iChancy من الرصيد النقدي، يتم إرفاق بونص لعب تلقائياً حسب نسبة يحددها المشرف.
- المبلغ المرسل إلى iChancy = مبلغ الشحن النقدي + البونص المرفق.
- البونص المرفق يُسجل كبونص نشط داخل اللعبة في `game_bonus_amount`.
- عند السحب من اللعبة، يتم خصم بونص اللعب النشط أولاً، ثم يضاف الصافي فقط إلى رصيد البوت.

## إعدادات المشرف الجديدة في Mini App

من لوحة إعدادات المشرف يمكن التحكم في:

- تفعيل/إيقاف إرفاق بونص اللعب عند شحن اللعبة.
- نسبة إرفاق بونص اللعب `%`.

هذا النظام معزول عن Flash Bonus، ولا يغير منطق فلاش بونص.

## حماية إضافية

- لا يُسمح بأن تتجاوز نسبة إرفاق بونص اللعب عمولة السحب.
- في حال فشل API أثناء شحن اللعبة، يتم إرجاع الرصيد النقدي والبونص إلى مكانهما الصحيح، ولا يتحول البونص إلى رصيد نقدي.
- تم تعطيل endpoint تحويل البونص المستقل إلى اللعبة؛ البونص يُرفق فقط مع شحن لعبة حقيقي.

---

# إصلاح تتبع بونص اللعب بعد الاختبار

بعد اختبار بونص شام كاش على إيداع صغير، تم تعزيز النظام كالتالي:

- جعل تحديثات `bonus_balance` و `bot_balance` تستخدم `COALESCE` حتى لا تضيع المكافأة إذا كانت قيمة الرصيد فارغة `NULL` لمستخدم قديم.
- عند شحن اللعبة، أصبحت رسالة التأكيد تعرض دائماً حالة البونص:
  - بونص مرفق فعلاً.
  - أو يوجد بونص لكن إرفاقه متوقف من الإدارة.
  - أو لا يوجد بونص متاح.
- هذا يساعد المشرف والمستخدم على معرفة سبب عدم إرفاق البونص فوراً.

---

# إصلاح افتراضي إعدادات إرفاق بونص اللعبة

بعد ظهور حالة أن بونص الإيداع يُقبل لكنه لا يُرفق عند شحن اللعبة، تم إصلاح حالة قواعد البيانات القائمة التي قد تكون فيها أعمدة:

- `game_bonus_enabled`
- `game_bonus_apply_percent`

بقيمة `NULL` بعد إضافة الأعمدة الجديدة.

الإصلاح:

- إذا كانت `game_bonus_enabled` فارغة، تُعامل كمفعّلة.
- إذا كانت `game_bonus_apply_percent` فارغة، تُعامل كـ 10% افتراضياً.
- عند تهيئة الجداول يتم تحديث سجل الإعدادات الافتراضي تلقائياً لهذه القيم.

ملاحظة: إذا كانت معاملة قديمة لم تُضف البونص فعلياً بسبب قيمة NULL قديمة، يجب تعويضها يدوياً مرة واحدة للمستخدم المتأثر.

---

# إصلاح صرف البونص نسبياً حسب مبلغ شحن اللعبة

تم تعديل منطق إرفاق البونص ليطابق القرار النهائي:

- عند قبول إيداع عليه بونص، يتم حفظ البونص في `bonus_balance` ويتم أيضاً حفظ مبلغ الإيداع المرتبط بهذا البونص في `bonus_base_balance`.
- عند شحن اللعبة:
  - إذا شحن المستخدم كامل مبلغ الإيداع المرتبط بالبونص، يحصل على كامل البونص في اللعبة.
  - إذا شحن نصفه، يحصل على نصف البونص.
  - إذا شحن ربعه، يحصل على ربع البونص.
- مثال: إيداع 20,000 وبونص 1,000:
  - شحن 20,000 => يضاف 1,000، الإجمالي 21,000.
  - شحن 10,000 => يضاف 500، الإجمالي 10,500.
- تم استخدام `game_bonus_apply_percent` فقط كاحتياط للبونصات القديمة أو غير المرتبطة بإيداع.
- عند فشل شحن اللعبة، يرجع الرصيد النقدي والبونص وقاعدة البونص إلى وضعها الصحيح.

---

# تطبيق Flash Bonus كعرض بونص مؤقت

تم تطبيق منطق Flash Bonus داخل حساب بونص الإيداع:

- Flash Bonus يعامل مثل البونص العادي تماماً.
- يطبق فقط إذا كان نشطاً ضمن مدته.
- يطبق على طريقة الدفع المحددة أو `all`.
- لا يتراكم مع البونص العادي؛ يتم اختيار أعلى بونص منطبق فقط.
- إذا كان الفلاش أعلى من العرض العادي، يحصل المستخدم على الفلاش.
- إذا كان العرض العادي أعلى، يحصل المستخدم على العرض العادي.
- البونص الناتج يدخل إلى `bonus_balance` ويرتبط بـ `bonus_base_balance` مثل أي بونص عادي، ثم يُصرف نسبياً عند شحن اللعبة.

مثال:
- عرض عادي ShamCash 5%.
- Flash Bonus ShamCash 8%.
- إيداع 20,000 => البونص = 1,600 وليس 1,000، لأنه اختار الأعلى.

---

# تحديث عجلة الحظ حسب منطق البونصات

تم تثبيت منطق عجلة الحظ كالتالي:

- أي إيداع `deposit_bot` مقبول ويطابق الحد الأدنى للإيداع في البوت يصبح مؤهلاً للدوران.
- لم تعد أهلية العجلة تعتمد على حد منفصل `wheel_min_deposit`؛ بل تعتمد على `min_deposit_syp` من إعدادات الإيداع.
- مكافأة العجلة تعامل معاملة البونص العادي والفلاش:
  - تضاف إلى `bonus_balance`.
  - تضاف قيمة الإيداع إلى `bonus_base_balance` حتى تُصرف المكافأة نسبياً عند شحن اللعبة.
- الدوران يبقى مرة واحدة فقط لكل إيداع بفضل جدول `wheel_spins`.
- السقف الأعلى للجائزة `wheel_max_reward` بقي فعالاً للحماية من الجوائز الكبيرة جداً.

---

# اعتماد تصميم عجلة PINGO السوداء/الذهبية

تم اعتماد العجلة الجديدة بالترتيب الثابت:

`PINGO - 0% - 1% - 2% - 0% - 3% - 4% - 0%`

## ما تم تطبيقه

- الخانات أصبحت ثابتة من ناحية قيمة الربح/النوع.
- يمكن للمشرف من لوحة التحكم تعديل ثقل/احتمال ظهور كل خانة فقط.
- يمكن جعل وزن أي خانة `0` لتعطيل ظهورها.
- خانة PINGO لا تضيف رصيداً تلقائياً، بل ترسل تنبيهاً للإدارة وتظهر للمستخدم كجائزة خاصة.
- التصميم في Mini App أصبح أسود/ذهبي:
  - خلفية سوداء.
  - فواصل ذهبية.
  - كتابة ذهبية.
  - خانة PINGO ذهبية بكتابة سوداء.
- تم تثبيت وقوف المؤشر في منتصف الخانة الفائزة باستخدام `(index + 0.5) * angle`.

## تنبيه الإدارة عند PINGO

عند وقوع المستخدم على PINGO يتم إرسال رسالة إلى قناة السجلات/المشرفين تتضمن:

- بيانات المستخدم.
- رقم الإيداع.
- مبلغ الإيداع.
- اقتراح التواصل معه وإرسال كود هدية.

---

# إصلاح كاش Telegram WebView للعجلة الجديدة

عند ظهور العجلة القديمة رغم رفع الكود، السبب الأغلب هو كاش Telegram WebView أو كاش المتصفح.

تم تطبيق:

- إضافة رقم نسخة إلى رابط Mini App للمستخدم:
  `/user-app?v=pingo-wheel-v3-20260708`
- إضافة رقم نسخة إلى رابط لوحة التحكم:
  `/dashboard?v=pingo-wheel-v3-20260708`
- إرسال صفحات HTML برؤوس منع الكاش:
  - `Cache-Control: no-store, no-cache, must-revalidate, max-age=0`
  - `Pragma: no-cache`
  - `Expires: 0`

هذا يجبر Telegram/المتصفح على تحميل نسخة العجلة الجديدة بدل النسخة القديمة.

---

# نظام أرباح الإحالات القابلة للسحب

تم تحويل منطق الإحالات إلى نموذج أقرب للشراكة:

- الإحالة ما زالت تُسجّل من رابط الإحالة.
- الإحالة تصبح نشطة عند أول إيداع مقبول للمحال.
- تم إيقاف دفع عمولة الإيداع القديمة عند قبول الإيداع.
- تم إضافة رصيد مستقل للمحيل: `affiliate_balance`.
- تم إضافة جدول `affiliate_weekly_commissions` لمنع تكرار صرف نفس الأسبوع.
- عمولات الإحالات تُحسب أسبوعياً على أساس خسارة المحالين في اللعبة:
  `شحن اللعبة - سحب اللعبة - رصيد اللعبة الحالي`.
- إذا كانت خسارة المحال موجبة، يحصل المحيل على نسبة حسب عدد إحالاته النشطة:
  - 3 إحالات نشطة = 1%
  - 5 إحالات نشطة = 1.5%
  - 10 إحالات نشطة = 2%
- العمولة تضاف إلى `affiliate_balance` كرصد قابل للسحب.
- أُضيف زر في Mini App للمشرف لصرف عمولات الإحالات الأسبوعية بكبسة زر مع تقرير.
- يظهر رصيد أرباح الإحالات للمستخدم في Mini App والقائمة عند وجود رصيد.
- يمكن للمستخدم تحويل رصيد أرباح الإحالات إلى رصيد البوت من قائمة الإحالات ليصبح قابلاً للسحب عبر نظام السحب العادي.

---

# تحديث مكافآت VIP لتتوافق مع نظام البونص الجديد

تم تعديل مكافأة ترقية VIP كالتالي:

- عند ترقية المستخدم إلى طبقة VIP جديدة، تدخل مكافأة الترقية إلى `bonus_balance`.
- يتم أيضاً إضافة مبلغ الإيداع الذي سبب الترقية إلى `bonus_base_balance`.
- بهذا تصبح مكافأة VIP مثل البونص العادي والفلاش والعجلة:
  - إذا شحن المستخدم كامل مبلغ الإيداع الذي سبب الترقية، يحصل على كامل مكافأة VIP في اللعبة.
  - إذا شحن نصفه، يحصل على نصف مكافأة VIP.
- تم استخدام `COALESCE` لحماية المستخدمين القدامى من قيم NULL.

ملاحظة: لم يتم تفعيل حقل `current_bonus_pct` كبونص إضافي على كل إيداع بعد؛ بقي كما هو للعرض/النقاش لاحقاً.

---

# تفعيل بونص الإيداع المستمر حسب طبقة VIP

تم تفعيل الحقل الموجود في لوحة VIP: `بونص الإيداع %` ليعمل فعلياً مع كل إيداع.

## المنطق الجديد عند قبول الإيداع

- يتم حساب أفضل بونص عام أو Flash Bonus.
- يتم حساب بونص VIP حسب طبقة المستخدم الحالية قبل الإيداع.
- إذا تسبب الإيداع بترقية VIP، يتم احتساب مكافأة الترقية أيضاً.
- كل هذه القيم تُجمع وتدخل إلى `bonus_balance` مرة واحدة.
- يتم ربط إجمالي البونص بمبلغ الإيداع عبر `bonus_base_balance` مرة واحدة فقط، حتى يُصرف البونص نسبياً عند شحن اللعبة بشكل صحيح.

## مثال

إذا كان المستخدم في طبقة فارس +1% وأودع 100,000، ومعه عرض عام 5%:

- بونص العرض = 5,000
- بونص VIP = 1,000
- إجمالي البونص = 6,000

وإذا تسبب الإيداع بترقية ومكافأة 10,000، يصبح الإجمالي 16,000.

## ملاحظة

مكافأة الترقية لم تعد تُضاف عبر مسار منفصل بعد القبول حتى لا تتكرر قاعدة `bonus_base_balance`؛ بل تُحسب ضمن إجمالي بونص نفس الإيداع.

---

# تحديث منطق الكاش باك كاستحقاق يضاف عند أول شحن لعبة

تم تعديل الكاش باك حسب الاتفاق:

- الكاش باك = نسبة من حرق/خسارة المستخدم الأسبوعية.
- لا يُضاف مباشرة إلى `bonus_balance` ولا إلى رصيد نقدي.
- يُحفظ في حقل جديد: `cashback_pending_balance`.
- يظهر للمستخدم في Mini App ككاش باك مستحق سيُضاف تلقائياً عند أول شحن ناجح للعبة.
- عند شحن اللعبة بنجاح، يُضاف كامل الكاش باك المستحق إلى مبلغ الشحن المرسل إلى iChancy.
- بعد نجاح الشحن يتم تصفير `cashback_pending_balance`.
- الكاش باك المضاف إلى اللعبة يُضاف أيضاً إلى `game_bonus_amount` حتى يُخصم أولاً عند السحب مثل بونص اللعب.
- إذا فشل شحن اللعبة بعد الحجز، يرجع الكاش باك المستحق إلى `cashback_pending_balance` ولا يضيع.
- رسالة الكاش باك الأسبوعي للمستخدم أصبحت تسويقية وواضحة: تخبره أن لديه كاش باك مستحق وسيُضاف تلقائياً عند أول شحن لعبة ناجح.

---

# تحديث نظام الحضور اليومي إلى دورة تقدم بصري 30 يوم

تم تعديل الحضور اليومي حسب الاتفاق:

- الحضور اليومي يتفعل بعد أول إيداع مقبول للمستخدم، أياً كانت قيمة الإيداع.
- لا يتم صرف مكافآت يومية مباشرة.
- يرى المستخدم تقدمه بصرياً داخل Mini App: عدد الأيام المنجزة من الدورة.
- إذا انقطع يوم واحد يعود التقدم للبداية.
- عند إكمال دورة الحضور بدون انقطاع، يتم فحص شرط مجموع إيداعات آخر 30 يوم.
- الحد الأدنى للإيداعات ومبلغ مكافأة إكمال الدورة وعدد أيام الدورة أصبحت من إعدادات Mini App للمشرف.
- إذا حقق المستخدم الشروط، تضاف المكافأة إلى `checkin_pending_balance`.
- مكافأة الحضور المستحقة تظهر للمستخدم كتسويق واضح، وتُضاف تلقائياً عند أول شحن لعبة ناجح.
- عند شحن اللعبة، يتم إرسال:
  `مبلغ الشحن + بونص اللعب + الكاش باك المستحق + مكافأة الحضور المستحقة`
- مكافأة الحضور المضافة للعبة تدخل في `game_bonus_amount` وتُخصم أولاً عند السحب مثل باقي البونصات.

---

# تحديث أكواد الهدايا من المشرف

تم اعتماد بادئات الأكواد الجديدة:

- كود لاعب إلى لاعب: `CAESAR-` بدون تغيير، ويُضاف إلى رصيد البوت القابل للسحب.
- كود كاش من المشرف: `CAESAR-CASH-` ويُضاف إلى `bot_balance` كرصد قابل للسحب.
- كود بونص من المشرف: `CAESAR-BONUS-` ويُضاف إلى `bonus_balance` كرصد مكافآت للعب.

## لوحة الأدمن

عند إنشاء كود هدية من المشرف، أصبح المشرف يختار أولاً:

- كود بونص للعب.
- كود كاش قابل للسحب.

ثم يدخل قيمة الكود، ويتم توليد البادئة المناسبة تلقائياً.

## الاسترداد

عند استرداد الكود:

- `CAESAR-BONUS-` يذهب إلى رصيد مكافآت اللعب.
- `CAESAR-CASH-` يذهب إلى رصيد البوت القابل للسحب.
- `CAESAR-` من لاعب إلى لاعب يبقى كما هو إلى رصيد البوت.

---

# تحديث جوائز مسابقات القيصر

تم تحديث نظام جوائز المسابقات ليستخدم بادئات أكواد الهدايا الجديدة:

## الأنواع الجديدة من لوحة Mini App

- كود بونص للعب: `bonus_code`
  - ينشئ كوداً يبدأ بـ `CAESAR-BONUS-`
  - عند الاسترداد يضاف إلى `bonus_balance`.

- كود كاش قابل للسحب: `cash_code`
  - ينشئ كوداً يبدأ بـ `CAESAR-CASH-`
  - عند الاسترداد يضاف إلى `bot_balance`.

## ملاحظات توافق

- تم إبقاء دعم `gift_code` القديم في الخلفية واعتباره كود بونص للعب.
- تم إبقاء دعم `bot_balance` و `bonus_balance` في الخلفية فقط، لكن واجهة Mini App تعرض الآن خيارَي الكود الواضحين.

---

# ضبط قاعدة صرف بونص العجلة بدون مضاعفة الإيداع

تم تحسين منطق `bonus_base_balance` عند ربح العجلة:

- إذا كان نفس الإيداع قد أضاف قاعدة صرف مسبقاً بسبب بونص الإيداع/الفلاش/VIP، فإن جائزة العجلة تُضاف إلى `bonus_balance` فقط ولا تضيف `bonus_base_balance` مرة ثانية.
- إذا كان الإيداع لم يحصل على أي بونص سابق، فإن جائزة العجلة تضيف `bonus_base_balance` بقيمة الإيداع، حتى يستطيع المستخدم صرف جائزة العجلة نسبياً عند شحن نفس مبلغ الإيداع.
- تمت إضافة عمود `bonus_base_added_syp` إلى جدول `transactions` لتتبع هل أضافت هذه المعاملة قاعدة صرف للبونص أم لا.

بهذا لا يضطر المستخدم لشحن ضعف الإيداع للحصول على مكافآت جاءت من نفس الإيداع.

---

# دمج التحقق التلقائي لإيداعات Syriatel Cash

تمت إضافة تكامل Syriatel Cash API:

## الإعدادات الجديدة

في متغيرات البيئة:

```env
SYRIATEL_API_TOKEN=
SYRIATEL_API_QUERY=
SYRIATEL_AUTO_VERIFY_ENABLED=true
SYRIATEL_AUTO_APPROVE_ENABLED=true
```

- `SYRIATEL_API_TOKEN`: توكن API، لا يوضع في GitHub.
- `SYRIATEL_API_QUERY`: رقم Syriatel Cash أو secret code حسب اشتراك API.
- `SYRIATEL_AUTO_VERIFY_ENABLED`: تفعيل التحقق التلقائي.
- `SYRIATEL_AUTO_APPROVE_ENABLED`: إذا نجح التحقق يتم قبول الإيداع تلقائياً.

## المنطق

عند إيداع Syriatel Cash:

- يستخدم البوت رقم العملية أو رقم الهاتف الذي يرسله المستخدم في حقل الإثبات.
- يستدعي `/IncomingHistory` من API.
- يبحث عن حوالة مطابقة:
  - رقم العملية أو رقم المرسل.
  - المبلغ.
  - حالة ناجحة `status = 1`.
  - رقم العملية غير مستخدم سابقاً.
- عند التطابق، يقبل الإيداع تلقائياً باستخدام نفس منطق البونصات وVIP والإحالات.
- إذا لم يتم التحقق، يبقى الطلب pending للمراجعة اليدوية.

## الأمان

- يتم تخزين رقم العملية في `transactions.external_ref`.
- يتم منع استخدام نفس رقم العملية مرتين.
- إذا فشل API أو rate limit، لا يتم رفض الطلب؛ يبقى للمراجعة اليدوية.

---

# تحديث التحكم بأتمتة Syriatel Cash من لوحة المشرف

تمت إضافة تحكم مباشر من Mini App في أتمتة إيداعات Syriatel Cash:

## أوضاع التشغيل

- `off`: يدوي بالكامل، لا يتم استدعاء API.
- `verify_only`: يتحقق من API ويضيف ملاحظة/إشعار، لكن المشرف يقبل يدوياً.
- `auto_approve`: يتحقق من API، وإذا وجد حوالة مطابقة يقبل الإيداع تلقائياً.

## قناة خاصة

تمت إضافة حقل قناة إشعارات Syriatel Auto:

- يمكن ضبط `syriatel_auto_channel_id` من Mini App.
- طلبات Syriatel وإشعارات التحقق تُرسل إلى هذه القناة إذا تم ضبطها.
- إذا لم تُضبط قناة خاصة، يبقى السلوك الافتراضي.

## متغيرات البيئة

تمت إضافة fallback في متغيرات البيئة:

```env
SYRIATEL_AUTO_MODE=off
SYRIATEL_AUTO_CHANNEL_ID=
```

ملاحظة: التوكن ورقم الاستعلام ما زالا في البيئة فقط:

```env
SYRIATEL_API_TOKEN=
SYRIATEL_API_QUERY=
```

---

# تحسين استهلاك Neon Free / تقليل CU-hours

تمت إضافة إعدادات لتخفيف استهلاك Neon على الخطة المجانية:

## متغيرات البيئة الجديدة

```env
WATCHDOG_ENABLED=true
WATCHDOG_INTERVAL_SECONDS=1800
WATCHDOG_AGENT_BALANCE_DB_UPDATE_SECONDS=3600
DB_POOL_MINCONN=1
DB_POOL_MAXCONN=5
DB_VALIDATE_CONNECTION=false
```

## التغييرات

- تقليل Watchdog الافتراضي من 5 دقائق إلى 30 دقيقة.
- إمكانية تعطيل Watchdog بالكامل عبر `WATCHDOG_ENABLED=false`.
- تقليل تحديث رصيد الكاشيرة في قاعدة البيانات؛ لا يتم الحفظ إلا عند تغير الرصيد أو بعد مدة محددة.
- تخفيض الحد الأقصى لاتصالات قاعدة البيانات من 20 إلى 5 افتراضياً.
- تعطيل فحص `SELECT 1` عند كل اتصال افتراضياً لتقليل الاستعلامات الزائدة.

## ملاحظة

استخدام UptimeRobot مع Watchdog كل 5 دقائق كان يبقي Render نشطاً، وبالتالي تبقى Neon مستيقظة وتستهلك CU-hours. الإعدادات الجديدة مناسبة أكثر للخطة المجانية.

---

# إصلاح تنبيه رصيد الكاشيرة عند انتهاء/تعطل جلسة iChancy

بعد تحسينات Neon ظهر أن جلب رصيد الكاشيرة قد يرجع 0 عند فشل API أو انتهاء الجلسة، مما يسبب تصفير الرصيد المخزن وإطلاق تنبيه خاطئ.

تم الإصلاح:

- `get_admin_balance()` أصبح يعيد `None` عند فشل القراءة أو عدم وجود حقل `balance` في الرد، بدلاً من إعادة 0.
- الـ Watchdog لم يعد يحدّث `agent_balance` في قاعدة البيانات إلا إذا حصل على قراءة صالحة.
- فحص تنبيه رصيد الكاشيرة لا يعمل إلا بعد جلب رصيد حي صالح من iChancy.

بهذا لا يتم اعتبار فشل الجلسة أو رد API الفارغ كرصيد 0.

---

# تحسين تجديد جلسة iChancy تلقائياً

بما أنه لا يوجد Cloudflare حالياً على موقع الوكلاء، تم تحسين منطق Auto-login:

- توحيد كشف انتهاء الجلسة عبر دالة داخلية تشمل: `unauthorized`, `expired`, `session_expired`, `ex`, `not_authorized`.
- عند نجاح تسجيل الدخول التلقائي يتم تحديث `last_cookie_update` مع حفظ الكوكيز.
- تحسين سحب الأموال من اللاعب ليحاول إعادة تسجيل الدخول عند HTTP 401/403 مثل الشحن.
- إذا فشل الشحن/السحب بعد إعادة تسجيل الدخول، يتم تسجيل رد API في السجلات بدل الاكتفاء بـ False.

هذا يساعد البوت على تجديد PHPSESSID تلقائياً عند انتهاء الجلسة طالما لا توجد حماية Cloudflare/Turnstile وبيانات AGENT_USERNAME/AGENT_PASSWORD صحيحة.

---

# إصلاح نجاح شحن اللعبة الكاذب + تحسين Auto-login

تمت معالجة مشكلتين ظهرتا في السجلات:

## 1. Watchdog كان يستدعي `login_agent` غير موجودة

تمت إضافة wrapper عام:

```python
async def login_agent()
```

ليستخدم دالة `_login_agent()` الداخلية.

## 2. البوت كان يعتبر رد iChancy نجاحاً حتى لو لم يتغير رصيد اللاعب

تم تعديل شحن/سحب اللعبة ليجلب رصيد اللاعب قبل وبعد العملية ويتأكد من تغير الرصيد.

- إذا زاد الرصيد بعد الشحن: نجاح مؤكد.
- إذا لم يزد الرصيد رغم أن API أعاد نتيجة تبدو ناجحة: لا يتم إعلان النجاح.
- في الحالة غير المؤكدة، تبقى المعاملة `pending` للمراجعة اليدوية ولا يتم رد الرصيد تلقائياً، حتى لا يحدث تضارب إذا كان الشحن تم فعلاً لكن الرصيد تأخر بالتحديث.

## 3. تحسين قراءة رصيد اللاعب

تم تحسين استخراج حقل `balance` من أشكال مختلفة من رد API، وإرجاع `None` عند فشل القراءة بدلاً من صفر كاذب.

## 4. تحسين تسجيل الدخول التلقائي

تم توسيع كشف انتهاء الجلسة ليشمل:

```text
unauthorized, expired, session_expired, ex, not_authorized
```

وعند نجاح auto-login يتم تحديث `last_cookie_update`.
