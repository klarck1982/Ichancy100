# تقرير إصلاح طريقة الدفع في بانر فلاش البونص

**التاريخ:** 15 يوليو 2026

## المشكلة المؤكدة

كان منطق احتساب فلاش البونص يطبق المكافأة على طريقة الدفع الصحيحة، لكن بانر المستخدم كان يعرض نصًا ثابتًا يقول إن العرض على كل الإيداعات.

السبب:

1. `/api/user/me` لم يكن يرسل `payment_method` ضمن `flash_bonus`.
2. `renderFlash()` كان يستخدم عبارة ثابتة: «بونص إضافي على كل إيداع».

## الإصلاح

- توحيد مفاتيح طرق الدفع وتسمياتها العربية في backend.
- إضافة `payment_method` و`method_label` إلى Flash Bonus في API المستخدم والأدمن.
- رفض أي طريقة دفع غير موجودة في القائمة البيضاء عند إنشاء الفلاش.
- دعم المفاتيح القديمة وتحويلها إلى المفاتيح الحديثة تلقائيًا.
- جعل بانر المستخدم يعرض:
  - «على كل طرق الإيداع» عند اختيار `all`.
  - «حصريًا على إيداعات [اسم الطريقة]» عند اختيار طريقة محددة.
- إظهار طريقة الدفع في حالة الفلاش النشط وسجل الفلاشات في لوحة الأدمن.
- إضافة خيار `USDT (BEP20)` الذي كان ناقصًا من قائمة إنشاء الفلاش.

## طرق الدفع المغطاة

- كل طرق الإيداع.
- سيريتل كاش.
- MTN كاش.
- شام كاش (ليرة).
- شام كاش (دولار).
- USDT (TRC20).
- USDT (BEP20).

## المحاكاة

تم تشغيل:

```bash
python tests/simulate_flash_payment_method.py
```

النتائج:

- PASS: API المستخدم يرسل الطريقة والاسم العربي.
- PASS: API الأدمن يرفض الطريقة غير الصالحة.
- PASS: الحساب المالي يطبق الفلاش فقط على الطريقة المختارة.
- PASS: خيار «كل الطرق» ما زال يعمل على جميع الطرق المدعومة.
- PASS: نص البانر يطابق الطرق السبع.
- PASS: البانر الخاص بطريقة محددة لا يقول «كل طرق الإيداع».
- PASS: لوحة الأدمن تعرض الطريقة في الحالة والسجل.
- PASS: المحاكاة لم تتصل بقاعدة البيانات أو Neon أو Telegram أو الشبكة.

## ملاحظة أمان

لم يتم تغيير معادلة الفلاش أو آلية إضافة الرصيد. الإصلاح يخص تمرير طريقة الدفع وعرضها والتحقق من قيمتها فقط.
