# تقرير محاكاة اختصارات Mini App

**التاريخ:** 15 يوليو 2026  
**النطاق:** أزرار شحن الرصيد، سحب الرصيد، وإهداء الرصيد في داشبورد المستخدم.

## ما تم تطبيقه

- زر **شحن** يفتح الرابط الآمن:
  - `https://t.me/Caesar_Robert_bot?start=app_deposit`
- زر **سحب** يفتح:
  - `https://t.me/Caesar_Robert_bot?start=app_withdraw`
- زر **إهداء** يفتح:
  - `https://t.me/Caesar_Robert_bot?start=app_gift`
- زر **إحالات** يبقى داخل Mini App كما كان.
- تمت إضافة توجيه whitelisted في `/start` للرموز الثلاثة فقط.
- التوجيه يعيد استخدام مسارات البوت الحالية وFSM الحالي بدل إنشاء منطق مالي جديد.
- التحقق من الشروط يسبق فتح أي مسار.
- التحقق من حساب iChancy والرصيد والطلبات المعلقة بقي كما هو.

## نتائج المحاكاة غير المتصلة

تم تشغيل الاختبار البنيوي:

```bash
python tests/simulate_miniapp_shortcuts.py
```

والنتائج:

- PASS: توجيه رموز backend الثلاثة.
- PASS: محاكاة `Telegram.WebApp.openTelegramLink` في صفحتي Mini App.
- PASS: محاكاة fallback للمتصفح في الصفحتين.
- PASS: رفض أي action غير موجود في القائمة البيضاء.
- PASS: فحص الشروط يسبق توجيه الاختصار.
- PASS: دوال فتح المسارات لا تحتوي أي عملية خصم/إضافة/إنشاء معاملة مالية.
- PASS: JavaScript الخاص بالاختصارات لا يستدعي API أو Neon.

كما تم تشغيل محاكاة Runtime مع aiogram وحالة FSM ورسائل وقاعدة بيانات وهمية بالكامل:

```bash
python tests/simulate_runtime_flows.py
```

والنتائج:

- PASS: اختصار الشحن يصل إلى حالة اختيار عملة الإيداع.
- PASS: اختصار السحب يصل إلى حالة اختيار بوابة السحب بالليرة.
- PASS: اختصار الإهداء يصل إلى حالة إدخال مبلغ الهدية.
- PASS: موزّع `/start` يوجه الإجراءات الثلاثة إلى الدوال الصحيحة.
- PASS: رفض الإجراء غير المعروف.
- PASS: حماية الطلب المعلق ما زالت تمنع إيداعًا مكررًا.
- PASS: شرط تسجيل حساب iChancy ما زال فعالًا.
- PASS: قاعدة البيانات الوهمية عرضت عمليات قراءة فقط، ولم تحتوِ أي دالة كتابة مالية.

## ضمانات الأمان في هذا التحديث

- الضغط على الاختصار لا يخصم ولا يضيف رصيدًا.
- لا تُنشأ معاملة مالية عند فتح الرابط.
- تبدأ المعاملة فقط لاحقًا داخل مسار البوت الحالي وبعد إدخالات وتأكيدات المستخدم.
- لا يوجد Polling أو استهلاك دوري جديد.
- لا توجد متغيرات Environment أو تعديلات قاعدة بيانات مطلوبة.

## فحوصات الإصدار الإضافية

- نجح `compileall` لجميع ملفات Python.
- نجح `pyflakes` للملفات المعدلة وملفات المحاكاة دون تحذيرات.
- نجح فحص Syntax لكل JavaScript مضمّن باستخدام Node.
- لا توجد معرفات HTML مكررة في صفحتي Mini App.
- تطابقت صورة الشعار المضمّنة مع ملف PNG الأصلي.
- نجح فحص المسافات والأخطاء عبر `git diff --check`.

## الاختبار الحقيقي بعد النشر

بعد اكتمال Deploy، افتح Mini App من أحدث زر «لوحة القيصر» ثم اختبر الأزرار الثلاثة بحساب تجريبي. يكفي التأكد من ظهور أول شاشة صحيحة لكل مسار؛ لا يلزم إكمال أي معاملة مالية.
