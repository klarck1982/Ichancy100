# محاكاة نظام المكافآت بعد التعديلات

> هذه محاكاة منطقية للأرصدة وليست اتصالاً حياً بـ iChancy أو PostgreSQL.

## الفحوصات التقنية

تم تشغيل:

```bash
python -m compileall -q .
python -m pyflakes .
```

والنتيجة سليمة.

---

## السيناريو 1 — إيداع 20,000 + بونص 5% فقط

### قبول الإيداع

- الإيداع: 20,000
- بونص الإيداع 5%: 1,000

بعد القبول:

```text
bot_balance          = 20,000
bonus_balance        = 1,000
bonus_base_balance   = 20,000
```

### شحن اللعبة بـ 20,000

```text
نسبة الشحن من القاعدة = 20,000 / 20,000 = 100%
البونص المرفق = 1,000
الإجمالي إلى اللعبة = 21,000
```

بعد نجاح الشحن:

```text
bot_balance          = 0
bonus_balance        = 0
bonus_base_balance   = 0
game_bonus_amount    = 1,000
رصيد الكاشيرة        = 500,000 - 21,000 = 479,000
```

### لو سحب فوراً 21,000 من اللعبة

```text
المسحوب من iChancy       = 21,000
خصم بونص نشط             = 1,000
الصافي إلى bot_balance   = 20,000
```

الكاشيرة ترجع:

```text
479,000 + 21,000 = 500,000
```

لا يوجد استغلال للبونص.

---

## السيناريو 2 — إيداع 20,000 + بونص 5% + عجلة 1%

### قبول الإيداع

- الإيداع: 20,000
- بونص الإيداع 5%: 1,000

```text
bot_balance          = 20,000
bonus_balance        = 1,000
bonus_base_balance   = 20,000
transaction.bonus_base_added_syp = 20,000
```

### العجلة ربحت 1%

- جائزة العجلة: 200

لأن نفس الإيداع أضاف قاعدة صرف مسبقاً، العجلة لا تضيف `bonus_base_balance` مرة ثانية.

```text
bonus_balance        = 1,200
bonus_base_balance   = 20,000
```

### شحن اللعبة بـ 20,000

```text
نسبة الشحن = 20,000 / 20,000 = 100%
البونص المرفق = 1,200
الإجمالي إلى اللعبة = 21,200
```

بعد نجاح الشحن:

```text
bot_balance          = 0
bonus_balance        = 0
bonus_base_balance   = 0
game_bonus_amount    = 1,200
رصيد الكاشيرة        = 500,000 - 21,200 = 478,800
```

### لو سحب فوراً 21,200 من اللعبة

```text
المسحوب من iChancy       = 21,200
خصم بونص نشط             = 1,200
الصافي إلى bot_balance   = 20,000
```

الكاشيرة:

```text
478,800 + 21,200 = 500,000
```

لا يوجد تحويل للبونص إلى كاش.

---

## السيناريو 3 — إيداع 100,000 مع فلاش + VIP + عجلة + كاش باك + حضور

### الفرضيات

```text
رصيد الكاشيرة الابتدائي = 500,000
إيداع ياسمين = 100,000
بونص عادي = 5%
Flash Bonus = 8%
VIP = 1%
عجلة = 3%
كاش باك مستحق سابق = 3,000
مكافأة حضور مستحقة = 20,000
```

### عند قبول الإيداع

يتم اختيار الأعلى بين العادي والفلاش:

```text
Flash 8% = 8,000
VIP 1% = 1,000
إجمالي بونص القبول = 9,000
```

بعد قبول الإيداع:

```text
bot_balance        = 100,000
bonus_balance      = 9,000
bonus_base_balance = 100,000
```

### بعد العجلة 3%

```text
جائزة العجلة = 3,000
bonus_balance = 12,000
bonus_base_balance = 100,000  ← لا تتضاعف القاعدة
```

### عند شحن اللعبة بـ 100,000

```text
البونص المرتبط بالإيداع = 12,000
الكاش باك المستحق = 3,000
مكافأة الحضور المستحقة = 20,000
الإجمالي إلى اللعبة = 100,000 + 12,000 + 3,000 + 20,000 = 135,000
```

بعد نجاح الشحن:

```text
bot_balance                = 0
bonus_balance              = 0
bonus_base_balance         = 0
cashback_pending_balance   = 0
checkin_pending_balance    = 0
game_bonus_amount          = 35,000
رصيد الكاشيرة              = 500,000 - 135,000 = 365,000
```

---

## الحالة A — ياسمين خسرت كل رصيد اللعبة

```text
رصيد اللعبة النهائي = 0
سحب من اللعبة = 0
```

خسارة ياسمين الأسبوعية لأغراض الإحالة:

```text
deposit_to_game - withdraw_from_game - current_game_balance
= 135,000 - 0 - 0
= 135,000
```

إذا أحمد يستحق 2%:

```text
affiliate_balance لأحمد += 2,700
```

رصيد الكاشيرة المباشر يبقى:

```text
365,000
```

لكن محاسبياً، حرق 135,000 قد يولد Revenue للوكيل حسب نسبة iChancy.

مثلاً إذا agent_revenue_percent = 30%:

```text
Revenue تقديري = 40,500
```

---

## الحالة B — ياسمين ربحت وصار رصيدها 180,000 ثم سحبت كله

السحب من iChancy:

```text
180,000
```

خصم البونص النشط:

```text
game_bonus_amount = 35,000
الصافي إلى bot_balance = 180,000 - 35,000 = 145,000
```

الكاشيرة بعد السحب من اللعبة:

```text
365,000 + 180,000 = 545,000
```

إذا سحبت ياسمين من البوت مع عمولة 10%:

```text
bot_balance = 145,000
عمولة السحب = 14,500
الصافي للمستخدم = 130,500
```

خسارة ياسمين الأسبوعية لأغراض الإحالة:

```text
135,000 - 180,000 - 0 = -45,000
```

بما أنها ربحت:

```text
لا توجد عمولة إحالة لأحمد
```

---

## الخلاصة

1. البونص لا يتحول إلى كاش مباشر.
2. عند السحب من اللعبة يتم خصم `game_bonus_amount` أولاً.
3. فلاش بونص والعادي لا يتراكمان؛ يتم اختيار الأعلى.
4. VIP يضاف فوق أعلى بونص عام/فلاش.
5. العجلة لا تضاعف `bonus_base_balance` لنفس الإيداع.
6. الكاش باك والحضور يضافان عند أول شحن لعبة ناجح.
7. الإحالة تُدفع فقط إذا خسر المحال في اللعبة.

## نقطة تحتاج اختبار حي

يجب اختبار سيناريو: إيداع + بونص + عجلة ثم شحن اللعبة، والتأكد أن رسالة التأكيد تعرض الإجمالي الصحيح.
