# 🗄️ إعداد مراقبة Neon Metrics

## الخطوات المطلوبة

### 1. إضافة المتغيرات في Render

اذهب إلى:
**Render Dashboard → Your Service → Environment**

أضف المتغيرات التالية:

```env
NEON_API_KEY=your_neon_api_key_here
NEON_PROJECT_ID=your_project_id_here
```

> ⚠️ **تحذير أمني:** لا تكتب مفتاح Neon الحقيقي داخل أي ملف في المستودع.
> ضعه فقط في **Render → Environment**. إذا سبق وتم كشف مفتاح، ألغِه فوراً من
> Neon Console وأنشئ بديلاً بأقل صلاحية ممكنة.

> ℹ️ الخطة المجانية تستخدم endpoint المشروع `GET /projects/{id}`، لذلك
> `NEON_ORG_ID` **غير مطلوب**؛ يكفي `NEON_API_KEY` و `NEON_PROJECT_ID`.

### 2. إعادة النشر (Deploy)

بعد إضافة المتغيرات، اضغط **Manual Deploy** أو انتظر الـ auto-deploy.

### 3. اختبار الميزة

1. افتح **لوحة تحكم القيصر** (Mini App)
2. اضغط **المزيد** → **حالة النظام**
3. ستظهر بطاقة **Neon Database** مع:
   - Compute (ساعات)
   - Storage (GiB)
   - Transfer (GiB)
   - نسب الاستخدام %
   - **تنبيه تلقائي** عند تجاوز 70%

## ملاحظات مهمة

- البيانات تُحدث كل 15 دقيقة تقريباً من Neon، والبوت يخزّنها (كاش) 15 دقيقة لتقليل الطلبات.
- الحدود المستخدمة هي حدود الخطة المجانية: **100 CU-hours** / **0.5 GB تخزين** / **5 GB نقل** لكل مشروع شهرياً.
- على الخطة المجانية نستخدم `GET /projects/{id}` (وليس `consumption_history/v2` المخصص للخطط المدفوعة).
- إذا أردت تغيير الحدود، عدّل قاموس `FREE_TIER_LIMITS` في ملف `neon_metrics.py`.
- التخزين يُحسب كـ **الحجم اللحظي الفعلي** (مجموع `logical_size` من كل الفروع عبر `GET /projects/{id}/branches`) لأنه ما يُقاس عليه الحد المجاني 0.5 GB — وليس byte-hours.
- Compute و Transfer تُصفَّر عند بداية كل دورة فوترة وتُحدَّث بتأخير (~15 دقيقة، وقد تصل لساعة)، لذا القيم الصغيرة/الصفرية في بداية الدورة أو مع الاستهلاك الخفيف طبيعية.
- إذا فشل جلب Neon، يعرض البوت آخر بيانات مخزّنة مع وسم «بيانات مخزّنة» بدل إظهار خطأ.

## الأمان

- الـ API Key يجب أن يكون **Read Only**.
- لا تشارك الـ API Key مع أحد.