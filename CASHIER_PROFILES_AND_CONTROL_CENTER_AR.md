# نظام مناوبة المشرفين ومركز التحكم

## ملفات المشرفين

يستطيع الأدمن إضافة أي عدد من ملفات المشرفين. كل ملف يحتوي على:

- الاسم.
- Telegram ID اختياري للربط والتدقيق.
- عنوان شام كاش SYP.
- عنوان شام كاش USD.
- رقم/عناوين سيريتل كاش.
- رقم MTN كاش.

لا يوجد مشرف ثانٍ افتراضي. عند أول تشغيل يستمر البوت باستخدام عناوين `payment_settings` أو Render حتى يضيف الأدمن ملفًا ويفعّله.

## التبديل الآمن

التبديل يحدّث `active_cashier_profile_id` فقط داخل معاملة ذرية واحدة، ثم يسجل العملية في `cashier_switch_audit`.

- جميع حسابات `ADMIN_IDS` تستطيع التبديل.
- إشعارات الإيداع تبقى في قناة الإيداعات الحالية فقط.
- يتم إلغاء كاش إعدادات البوت فورًا بعد التبديل.
- طرق شام SYP/USD وسيريتل وMTN تستخدم الملف النشط.
- USDT يحتفظ بالعناوين القديمة/Render لأنه خارج ملف المناوبة المتفق عليه.

## تثبيت العنوان على الطلب

عندما يرى المستخدم عنوان الإيداع، يحفظ الـFSM:

- رقم ملف المشرف.
- اسم المشرف.
- العنوان الذي ظهر للمستخدم.

وعند إنشاء الطلب تُحفظ القيم في سجل `transactions`. لذلك إذا تغير المشرف أثناء إكمال المستخدم للعملية، يبقى الطلب مرتبطًا بالعنوان القديم الذي شاهده المستخدم.

## مفاتيح الطوارئ

تمت إضافة:

- `maintenance_mode`
- `deposits_enabled`
- `withdrawals_enabled`
- `game_transfers_enabled`

يتم فحص البوابات عند بدء العملية وقبل التأكيد النهائي أيضًا، لمنع عملية بدأت قبل الإيقاف من المرور بعد إغلاق البوابة.

## جداول وأعمدة قاعدة البيانات

- جدول `cashier_profiles`.
- جدول `cashier_switch_audit`.
- `bot_settings.active_cashier_profile_id`.
- أعمدة الطوارئ الأربعة في `bot_settings`.
- `transactions.cashier_profile_id`.
- `transactions.cashier_profile_name`.
- `transactions.payment_destination`.

يتم إنشاء الجداول والأعمدة تلقائيًا عند Deploy عبر `DatabaseManager.create_tables()`.
