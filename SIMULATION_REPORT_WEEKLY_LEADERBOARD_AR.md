# 🏆 لوحة المتصدرين الأسبوعية — تقرير التنفيذ والمحاكاة (Update 18)

## 1. ما الجديد؟

مسابقة أسبوعية بجوائز تلقائية مبنية على **دوران المراهنات الحقيقي** لكل لاعب في iChancy — وليس مجرد ترتيب بالرصيد.

- الخيار `leaderboard_type = 'weekly'` الذي كان موجوداً في لوحة الميزات بلا تنفيذ، أصبح الآن يعمل فعلياً.
- ترتيب مباشر داخل البوت (زر 🏆 المتصدرون الأسبوعيون) + داخل Mini App.
- تسوية آلية كل أسبوع: **اثنين 00:05 بتوقيت سوريا**، مع أرشفة وصرف الجوائز وإشعار الفائزين والمشرفين.

## 2. لماذا هذا التصميم؟ (قرارات هندسية)

| القرار | البديل المرفوض | السبب |
|---|---|---|
| فترق `totalBet` التراكمي − خط أساس محلي | fлетр زمني في API الكازينو | الـ API غير موثّق: لا ضمان بدعم `between` على الإحصائيات |
| الجلب بالجملة (`getPlayersStatisticsPro` بصفحات 500) | `get_player_turnover` لكل لاعب | طلب واحد لكل 500 لاعب بدل مئات الطلبات |
| منطق الأسابيع/الترتيب في `telegram_bot/leaderboard.py` (خالص) | دمه في repository | قابلية اختبار مباشرة بدون DB/شبكة |
| التحديث كل 3 ساعات كحد أدنى (`LEADERBOARD_REFRESH_MINUTES`) | تحديث عند كل فتح للوحة | حماية API الوكيل من الرشق + توفير CU في Neon |
| `credited` بحارس `UPDATE ... WHERE credited=FALSE` | تعليم الصرف بعد `credit_balance_atomic` | منع الدفع المزدوج رياضياً حتى وسط الانقطاع |
| `lb_last_settled_week` + `UNIQUE(week_start, telegram_id)` | مجلد `settled` منفصل | طبقتا منع تكرار مستقلتان |

تصفية الأهلية: `weekly_turnover >= lb_min_weekly_turnover` (الحد **شامل**)، وصرف الجوائز عبر `credit_balance_atomic` الموجودة (نفس ضمانات المشروع الأصلية).

## 3. الملفات

```
telegram_bot/leaderboard.py                          (جديد) محرك المنطق الخالص
database/connection.py                 (+47)  جدولان + 6 أعمدة bot_settings
database/repository.py                 (+242) 14 دالة بيانات
ichancy_api/client.py                  (+49)  get_all_players_stats_bulk
telegram_bot/main.py                   (+237) المجدول + التسوية + إشعارات وضع Mini App
telegram_bot/handlers/menu.py          (+81)  زر/عرض المستخدم
telegram_bot/handlers/admin.py         (+251) قسم الإدارة + 4 حالات FSM
telegram_bot/keyboards/inline.py       (+1)   الزر
tests/simulate_weekly_leaderboard.py  (جديد) 37 اختبار منطق خالص
```

## 4. الأمان عند الفشل (Fidelity Guarantees)

| السيناريو | السلوك |
|---|---|
| جلسة iChancy منتهية | العميل يعيد الدخول تلقائياً (الآلية الموجودة) ثم يستكمل |
| API الوكيل متعثر | التحديث يُتخطّى ويبقى آخر لقطة — لا شيء يُمسح |
| انقطاع وسط التسوية | إعادة التسوية آمنة: `ON CONFLICT` للأرشفة + `claim` ذري للصرف |
| البوت مطفأ يوم الاثنين | تعويض انقطاع حتى 48 ساعة، بعدها تخطٍّ ويمكن «تسوية الآن» يدوياً |
| لاعب جديد منتصف الأسبوع | يبدأ بدلتا صفر من أول ظهور (لا ميزة ولا غبن) |
| تصفير عدادات iChancy | الدلتا السالب تُحتسب صفراً — لا قيم سالبة |

## 5. نتائج الاختبارات المنفذة فعلياً

| المستوى | الأداة | النتيجة |
|---|---|---|
| منطق خالص (أسابيع، نوافذ، ترتيب، جوائز، منع تكرار) | `tests/simulate_weekly_leaderboard.py` | ✅ **36/36** |
| SQL حقيقي على PostgreSQL 17: creating tables, upsert (حفظ baseline), ON CONFLICT idempotency, claim guard, التدوير, الإعدادات مع الكاش | سكربت تكامل | ✅ **37/37** |
| مسرح `settle_weekly_leaderboard` كاملاً (عميل محاكى + بوت حقيقي الاستدعاءات): صرف صحيح، إشعارات، تخطٍّ عند التكرار، تدوير، تجاهل غير المرتبطين | سكربت E2E | ✅ **17/17** |
| ترجمة Python لكل الملفات المعدّلة | `py_compile` | ✅ |

## 6. خطوات التفعيل بعد النشر

1. انشر الكود كالمعتاد (Render) — الجداول والأعمدة تُنشأ تلقائياً عند الإقلاع.
2. من `/admin` → 🏆 لوحة المتصدرين الأسبوعية → حدّد الجوائز 🥇🥈🥉 وحد التأهل.
3. اضغط «🗓️ تفعيل الوضع الأسبوعي» ثم «🔄 تحديث الإحصائيات الآن» لبناء اللقطات الأولى.
4. أول تسوية آلية: أول اثنين 00:05 بتوقيت سوريا بعد التفعيل.

متغير اختياري جديد:
```
LEADERBOARD_REFRESH_MINUTES=180   # فترة تحديث الإحصائيات (دقيقة، الحد الأدنى 30)
```

## 7. حدود معروفة

- دقة `totalBet` نفسها تعتمد على كازينو طرف ثالث (خارج سيطرة الكود).
- أسابيع يفوّتها انقطاع >48 ساعة تُتخطّى عمداً (تُدار يدوياً بزر التسوية).
