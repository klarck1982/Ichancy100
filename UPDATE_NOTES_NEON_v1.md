# 📦 تحديث Neon Metrics v1.0
**التاريخ:** 2026-07-06
**السبب الرئيسي:** إضافة مراقبة استخدام Neon Database مباشرة من لوحة تحكم القيصر (Mini App)

---

## ✅ الملفات المعدلة / المضافة

### 1. `neon_metrics.py` (ملف جديد)
**السبب:** 
- إنشاء وحدة مستقلة لجلب بيانات الاستهلاك من Neon Consumption API v2.
- تحسب Compute + Storage + Transfer + نسب الاستخدام.
- تدعم التنبيه عند تجاوز 70% و 90% من الحدود المجانية.

### 2. `telegram_bot/main.py`
**التعديلات:**
- استيراد `neon_metrics`
- إضافة قسم `neon` في `admin_health_handler`
- جلب البيانات وإرجاعها في JSON

**السبب:** 
- ربط بيانات Neon مع API الصحة الموجود (`/api/admin/health`).

### 3. `webapp/dashboard.html`
**التعديلات:**
- تحديث دالة `renderHealth()` بالكامل
- إضافة بطاقة Neon Database مع:
  - إحصائيات Compute / Storage / Transfer
  - نسب الاستخدام %
  - تنبيه تلقائي عند ≥70%

**السبب:** 
- عرض واضح وجميل للمشرف مع تنبيهات وقائية.

### 4. `env.example`
**التعديل:**
- إضافة 3 متغيرات جديدة في نهاية الملف:
  - `NEON_API_KEY`
  - `NEON_ORG_ID`
  - `NEON_PROJECT_ID`

**السبب:** 
- تسهيل إعداد الميزة على Render.

### 5. `NEON_METRICS_SETUP.md` (ملف جديد)
**السبب:** 
- دليل سريع للمستخدم لإضافة المتغيرات وإعادة النشر.

---

## 📋 ملخص التغييرات

| الملف                    | نوع التغيير     | السبب الرئيسي                          |
|--------------------------|------------------|---------------------------------------|
| `neon_metrics.py`        | جديد             | جلب بيانات Neon API                   |
| `main.py`                | تعديل            | ربط البيانات مع Health API            |
| `dashboard.html`         | تعديل كبير      | عرض النتائج + تنبيهات 70%             |
| `env.example`            | إضافة متغيرات   | تسهيل الإعداد                         |
| `NEON_METRICS_SETUP.md`  | جديد             | دليل التنصيب                          |

---

## 🚀 كيفية التطبيق

1. أضف المتغيرات الثلاثة في **Render Environment**
2. أعد نشر الخدمة (Deploy)
3. اختبر من Mini App → المزيد → حالة النظام

---

**ملاحظة:** هذا التحديث لا يؤثر على أي وظيفة سابقة، وهو إضافي فقط.