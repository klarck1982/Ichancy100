# تقرير إصلاح انتقال Mini App إلى مسارات البوت

**التاريخ:** 15 يوليو 2026

## المشكلة

`Telegram.WebApp.openTelegramLink()` كان يفتح رابط الشحن أو السحب أو الإهداء خلف نافذة Mini App، لكنه لا يغلق الداشبورد تلقائيًا. لذلك كان المستخدم يظن أن الزر لم يعمل حتى يصغّر الصفحة يدويًا.

## الإصلاح

- إظهار شاشة انتقال فورية باسم الخدمة المختارة.
- فتح رابط المسار في Telegram أولًا.
- إغلاق Mini App تلقائيًا بعد 420ms، وهي مهلة تسمح لTelegram باستلام الرابط قبل إغلاق WebView.
- إزالة شاشة الانتقال بعد 1800ms كاحتياط إذا لم يغلق العميل الصفحة.
- منع الضغط المزدوج حتى لا تُفتح روابط متعددة.
- الإبقاء على fallback المتصفح عند عدم توفر openTelegramLink.
- لا يوجد API أو Neon أو Render request جديد.

## المحاكاة

تم تشغيل:

```bash
python tests/simulate_miniapp_handoff.py
python tests/simulate_miniapp_shortcuts.py
```

النتائج:

- PASS: ظهور شاشة الانتقال فورًا وذكر الخدمة الصحيحة.
- PASS: فتح رابط Telegram قبل الإغلاق.
- PASS: استدعاء إغلاق Mini App بعد 420ms.
- PASS: منع الضغط المزدوج والروابط المكررة.
- PASS: إزالة شاشة الانتقال احتياطيًا إذا لم تُغلق الصفحة.
- PASS: نجاح صفحتي user_app وuser_app_pingo.
- PASS: عدم استخدام الشبكة أو Telegram أثناء المحاكاة.
