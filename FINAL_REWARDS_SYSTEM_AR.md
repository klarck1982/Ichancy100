# نظام المكافآت النهائي — Caesar Bot / Ichancy100

تاريخ النسخة: 2026-07-09

## الخلاصة

تم توحيد نظام المكافآت بحيث تكون المكافآت التسويقية غير قابلة للسحب المباشر، بل تُستخدم للعب وتُخصم عند السحب من اللعبة. أما أرباح الإحالات فأصبحت رصيداً مستقلاً قابلاً للسحب لأنها مبنية على خسارة المحالين في اللعبة.

---

## الأرصدة الرئيسية

- `bot_balance`: رصيد نقدي قابل للسحب.
- `bonus_balance`: بونص لعب يُرفق عند شحن اللعبة.
- `bonus_base_balance`: قاعدة صرف البونص نسبياً عند شحن اللعبة.
- `game_bonus_amount`: بونص دخل اللعبة ويُخصم أولاً عند السحب من اللعبة.
- `affiliate_balance`: أرباح إحالات قابلة للسحب.
- `cashback_pending_balance`: كاش باك مستحق ينتظر أول شحن لعبة ناجح.
- `checkin_pending_balance`: مكافأة حضور مستحقة تنتظر أول شحن لعبة ناجح.

---

## بونص الإيداع و Flash Bonus

- عند قبول الإيداع، يتم حساب أفضل بونص بين العرض العادي والفلاش.
- لا يتراكم العرض العادي مع الفلاش؛ المستخدم يأخذ الأعلى فقط.
- البونص يضاف إلى `bonus_balance`.
- مبلغ الإيداع يضاف إلى `bonus_base_balance`.

---

## VIP

- مكافأة الترقية تُضاف إلى `bonus_balance`.
- بونص الإيداع الخاص بطبقة VIP يُضاف فوق أفضل عرض عام/فلاش.
- إجمالي بونص نفس الإيداع يرتبط بقاعدة صرف واحدة حتى لا تتضاعف `bonus_base_balance`.

---

## عجلة الحظ + PINGO

- أي إيداع مقبول يطابق الحد الأدنى للإيداع في البوت يصبح مؤهلاً للدوران.
- الخانات ثابتة: `PINGO - 0% - 1% - 2% - 0% - 3% - 4% - 0%`.
- يمكن تعديل ثقل/احتمال كل خانة من لوحة التحكم.
- المؤشر يقف في منتصف الخانة الفائزة.
- PINGO لا يضيف رصيداً، بل يرسل تنبيهاً للإدارة للتواصل مع اللاعب.
- جائزة العجلة تُضاف إلى `bonus_balance`.
- إذا كان نفس الإيداع أضاف قاعدة صرف مسبقاً، لا تضيف العجلة قاعدة جديدة.

---

## شحن اللعبة

عند شحن اللعبة، يرسل النظام إلى iChancy:

```text
مبلغ الشحن النقدي
+ البونص المرفق من bonus_balance
+ الكاش باك المستحق
+ مكافأة الحضور المستحقة
```

بعد نجاح الشحن:

- يتم تصفير المستحقات المستخدمة.
- يتم زيادة `game_bonus_amount` بقيمة كل المكافآت التي دخلت اللعبة.

---

## السحب من اللعبة

عند السحب من اللعبة:

```text
الصافي إلى bot_balance = مبلغ السحب - game_bonus_amount المتاح
```

إذا كان السحب أقل من البونص النشط، لا يضاف شيء نقدي ويتم تخفيض البونص النشط فقط.

---

## الإحالات

- الإحالة تتفعل عند أول إيداع مقبول للمحال.
- لا توجد عمولة على الإيداع مباشرة.
- أرباح الإحالة تُحسب أسبوعياً من خسارة المحال في اللعبة.
- النسب:
  - 3 إحالات نشطة = 1%
  - 5 إحالات نشطة = 1.5%
  - 10 إحالات نشطة = 2%
- الأرباح تضاف إلى `affiliate_balance` وهي قابلة للسحب.
- يوجد زر في Mini App لصرف عمولات الإحالات الأسبوعية مع تقرير.

---

## Cashback

- الكاش باك نسبة من حرق/خسارة المستخدم الأسبوعية.
- لا يضاف إلى `bonus_balance` مباشرة.
- يضاف إلى `cashback_pending_balance`.
- يضاف تلقائياً إلى اللعبة عند أول شحن ناجح.

---

## الحضور اليومي

- يتفعل بعد أول إيداع مقبول.
- المستخدم يرى تقدمه بصرياً خلال دورة الحضور.
- لا توجد مكافآت يومية مباشرة.
- عند إكمال الدورة وتحقيق شرط الإيداعات الشهرية، تضاف المكافأة إلى `checkin_pending_balance`.
- تُضاف المكافأة إلى اللعبة عند أول شحن ناجح.

---

## أكواد الهدايا

- `CAESAR-`: كود لاعب إلى لاعب، يضاف إلى `bot_balance`.
- `CAESAR-CASH-`: كود كاش من المشرف، يضاف إلى `bot_balance`.
- `CAESAR-BONUS-`: كود بونص من المشرف، يضاف إلى `bonus_balance`.

---

## المسابقات

- جوائز المسابقات أصبحت:
  - كود بونص للعب: `CAESAR-BONUS-`
  - كود كاش قابل للسحب: `CAESAR-CASH-`

---

## الاختبارات الموصى بها بعد الديبلوي

1. إيداع + بونص فقط ثم شحن اللعبة.
2. إيداع + بونص + عجلة ثم شحن اللعبة.
3. فلاش أعلى من البونص العادي.
4. مستخدم VIP مع بونص إيداع.
5. سحب من اللعبة بعد دخول بونص.
6. صرف كاش باك ثم شحن اللعبة.
7. إكمال حضور ثم شحن اللعبة.
8. صرف أرباح إحالات أسبوعية.
9. استرداد أكواد `CAESAR-CASH` و `CAESAR-BONUS`.
