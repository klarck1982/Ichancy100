# تقرير محاكاة حزمة Production UX

**التاريخ:** 15 يوليو 2026  
**النطاق:** الأزرار الملكية، فلاش البونص العلوي، خصوصية الرصيد، حالة الطلب المعلق، تلميح تحديث الشعار، Safe Area، وإزالة مؤشر التطوير.

## التعديلات المطبقة

### الأزرار الملكية

- نقل أزرار شحن وسحب وإهداء وإحالات إلى أسفل بطاقة الرصيد مباشرة.
- استبدال الإيموجي بأيقونات SVG مضمّنة وثابتة بين Android وiPhone.
- إضافة ميداليات وإطار ذهبي وزجاج أسود ولمسات دلالية صغيرة.
- الحركة تحدث عند الدخول والضغط فقط، وتحترم إعداد تقليل الحركة.
- وظائف الاختصارات الآمنة من الإصدار السابق بقيت كما هي.

### فلاش البونص

- نقل فلاش البونص إلى أعلى الصفحة: تحت الشعار وقبل بطاقة الرصيد.
- لا يشغل مساحة عندما لا يوجد فلاش نشط.
- العداد يعمل محليًا كل ثانية من وقت الانتهاء المحمّل مسبقًا.
- لا ينفذ العداد أي طلب API أو قراءة Neon.

### تحسينات إضافية

- زر محلي لإظهار أو إخفاء الأرصدة مع حفظ الاختيار في `localStorage`.
- شريط للطلبات المعلقة مشتق من `recent_transactions` المحمّلة أصلًا.
- تلميح «اضغط على الشعار لتحديث الأرصدة» يظهر مرة واحدة فقط.
- إزالة مؤشر التطوير `pingo-wheel-v4-20260708` من نسخة الإنتاج.
- إضافة Safe Area لشريط التنقل السفلي.

## المحاكاة

تم تشغيل:

```bash
python tests/simulate_ux_pack.py
```

النتائج:

- PASS: وجود أربعة أزرار ملكية بأيقونات SVG في صفحتي Mini App.
- PASS: ترتيب الأزرار مباشرة تحت بطاقة الرصيد.
- PASS: فلاش البونص أعلى بطاقة الرصيد.
- PASS: عداد الفلاش لا يستدعي API أو Neon.
- PASS: إخفاء وإظهار الرصيد وحفظ الاختيار محليًا.
- PASS: تلميح تحديث الشعار يظهر مرة واحدة.
- PASS: استخراج الطلبات المعلقة من البيانات المحمّلة دون استعلام جديد.
- PASS: إزالة مؤشر التطوير.
- PASS: تفعيل Safe Area.

كما أعيد تشغيل اختبارات الاختصارات السابقة:

```bash
python tests/simulate_miniapp_shortcuts.py
python tests/simulate_runtime_flows.py
```

وبقيت مسارات الشحن والسحب والإهداء والحمايات المرتبطة بها ناجحة.

## فحوصات الإصدار النهائية

- نجح `compileall` لجميع ملفات Python.
- نجح `pyflakes` للملفات المعدلة والمحاكاة دون تحذيرات.
- نجح فحص HTML وعدم تكرار المعرفات.
- نجح تحليل CSS عبر `tinycss2` دون أخطاء.
- نجح فحص JavaScript عبر Node في صفحتي Mini App.
- تطابقت صورة الشعار المضمّنة مع ملف PNG.
- نجح `git diff --check`.

## ضمانات الاستهلاك والأمان

- لا توجد جداول أو أعمدة جديدة.
- لا توجد Environment Variables جديدة.
- لا يوجد Polling جديد.
- فلاش البونص يعد تنازليًا على الجهاز فقط.
- حالة الطلب المعلق تستخدم استجابة `/api/user/me` الحالية.
- خصوصية الرصيد والتلميح يستخدمان `localStorage` فقط.
- الضغط على اختصارات العمليات لا ينفذ معاملة مالية مباشرة.
